import assert from "node:assert/strict";
import { access, readFile } from "node:fs/promises";
const root = new URL("../", import.meta.url);
const pluginRoot = new URL("resources/plugins/builtin/hosts/", root);
const [manifestText,indexHtml,commands,appLib,pluginPanel,invokePolicy]=await Promise.all([readFile(new URL("plugin.json",pluginRoot),"utf8"),readFile(new URL("index.html",pluginRoot),"utf8"),readFile(new URL("src-tauri/src/commands.rs",root),"utf8"),readFile(new URL("src-tauri/src/lib.rs",root),"utf8"),readFile(new URL("src/components/PluginPanel.svelte",root),"utf8"),readFile(new URL("src/lib/pluginInvokePolicy.ts",root),"utf8")]);
const manifest=JSON.parse(manifestText);assert.equal(manifest.name,"hosts");assert.equal(manifest.main,"index.html");assert.deepEqual(manifest.permissions,["system.hosts.read","system.hosts.write"]);assert.equal(manifest.features[0].code,"hosts");await access(new URL(manifest.logo,pluginRoot));
assert.match(indexHtml,/window\.utools\.hosts\.read\(\)/);assert.match(indexHtml,/window\.utools\.hosts\.write\(editor\.value,true\)/);assert.match(indexHtml,/role="dialog" aria-modal="true"/);assert.doesNotMatch(indexHtml,/window\.confirm|confirm\(/);assert.match(commands,/pub async fn read_native_hosts/);assert.match(commands,/pub async fn write_native_hosts/);assert.match(commands,/Hosts update requires explicit confirmation/);assert.match(commands,/\.atools\/backups\/hosts/);assert.match(commands,/with administrator privileges/);assert.match(commands,/plugin\.hosts\.write/);assert.match(appLib,/commands::read_native_hosts/);assert.match(appLib,/commands::write_native_hosts/);assert.match(pluginPanel,/case 'readNativeHosts':/);assert.match(pluginPanel,/case "writeNativeHosts":/);assert.match(pluginPanel,/hosts:\s*\{/);assert.match(invokePolicy,/readNativeHosts: "system\.hosts\.read"/);assert.match(invokePolicy,/writeNativeHosts: "system\.hosts\.write"/);
