import assert from "node:assert/strict";import{access,readFile}from"node:fs/promises";const root=new URL("../",import.meta.url),pluginRoot=new URL("resources/plugins/builtin/todo/",root);const[manifestText,indexHtml,commands,appLib,pluginPanel,invokePolicy]=await Promise.all([readFile(new URL("plugin.json",pluginRoot),"utf8"),readFile(new URL("index.html",pluginRoot),"utf8"),readFile(new URL("src-tauri/src/commands.rs",root),"utf8"),readFile(new URL("src-tauri/src/lib.rs",root),"utf8"),readFile(new URL("src/components/PluginPanel.svelte",root),"utf8"),readFile(new URL("src/lib/pluginInvokePolicy.ts",root),"utf8")]);const manifest=JSON.parse(manifestText);assert.equal(manifest.name,"todo");assert.deepEqual(manifest.permissions,["todo.read","todo.write","todo.delete"]);assert.equal(manifest.features[0].code,"todo");await access(new URL(manifest.logo,pluginRoot));assert.match(indexHtml,/window\.utools\.todos\.list\(\)/);assert.match(indexHtml,/window\.utools\.todos\.save/);assert.match(indexHtml,/window\.utools\.todos\.delete\(item\.id,true\)/);assert.doesNotMatch(indexHtml,/localStorage|indexedDB/);assert.match(commands,/pub async fn list_native_todos/);assert.match(commands,/pub async fn save_native_todo/);assert.match(commands,/pub async fn delete_native_todo/);assert.match(commands,/plugin_data_put\(&plugin_id/);assert.match(commands,/plugin\.todo\.save/);assert.match(commands,/Todo deletion requires explicit confirmation/);assert.match(commands,/find\(\|plugin\| plugin\.name == NATIVE_TODO_PLUGIN_NAME\)/);assert.match(appLib,/commands::list_native_todos/);assert.match(appLib,/commands::save_native_todo/);assert.match(appLib,/commands::delete_native_todo/);assert.match(pluginPanel,/todos:\s*\{/);assert.match(invokePolicy,/listNativeTodos: "todo\.read"/);assert.match(invokePolicy,/saveNativeTodo: "todo\.write"/);assert.match(invokePolicy,/deleteNativeTodo: "todo\.delete"/);
