# ATools Web plugin template

This plugin runs in an isolated WebView and registers the same `echo` capability used by the Rust and Node templates.

```bash
node ../../../scripts/plugin-package.mjs --source . --output ./dist/atools-web-echo.zip
```
