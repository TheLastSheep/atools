# ATools Node plugin template

This plugin exposes `echo` over MCP stdio. Stdout is reserved for JSON-RPC messages; write diagnostics to stderr.

```bash
node ../../../scripts/plugin-package.mjs --source . --output ./dist/atools-node-echo.zip
```
